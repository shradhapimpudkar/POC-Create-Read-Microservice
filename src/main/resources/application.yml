server:
  port: 1000
spring:
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher
  application:
    name: poc-create-read
eureka:
  instance:
    prefer-ip-address: true
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true